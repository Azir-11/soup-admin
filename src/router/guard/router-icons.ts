import { renderIcon } from "@/utils";
import { BarChart, CheckmarkCircleOutline, GlobeOutline } from "@vicons/ionicons5";

//前端路由图标映射表
export const constantRouterIcon = {
  DashboardOutlined: renderIcon(BarChart),
  CheckmarkCircleOutline: renderIcon(CheckmarkCircleOutline),
  GlobeOutline: renderIcon(GlobeOutline),
};
